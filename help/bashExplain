create: create in the current folder a .sh file , giving it its right permission to be executed.

svnUpdateAll: update every folder in ~/svn, via svn update.

svnCommitAll: commit every folder in ~/svn, via svn commit, using the first parameter as -m parameter.

printdate: print the date in the format used in the changelog file (e.g. 2019.5.24 -> )

appendChangelog: append la stringa di printDate al file /home/giuseppe/Linux/changelog

getLocalIp: return the local ip on the wlp2s0 network card wifi
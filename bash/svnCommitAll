# !/bin/bash
if [ -z $1 ];
then
	#echo "Error: Insert commit"
	comment="$(printDate) Update"
else
	comment=$1
fi

#echo $comment
for d in /home/giuseppe/svn/*/; do echo "svn commit $d -m $comment" && svn commit $d -m "$comment" && echo "--------"; done